export const rows = 30
export const columns = 30
export const pixelHeight = 1000
export const pixelWidth = 1000
export const gameArea = {
  xMax: rows - 1,
  xMin: 0,
  yMax: columns - 1,
  yMin: 0,
}
